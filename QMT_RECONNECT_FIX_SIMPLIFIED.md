# QMT重连机制简化修复方案

## 问题分析
您说得对，问题不是时间段的问题，而是重连机制本身存在阻塞点。任何时候QMT重启都可能导致程序卡死。

## 根本问题
1. **`xt_trader.connect()` 调用可能阻塞** - 这是最可能的卡死点
2. **`xt_trader.stop()` 调用可能阻塞** - 停止连接时可能卡住
3. **重连过程中的等待时间过长** - 各种 `time.sleep()` 调用
4. **看门狗机制不够及时** - 5秒检查间隔太长

## 解决方案

### 1. 简化重连逻辑
- 移除了所有时间段相关的复杂逻辑
- 统一使用简单的重连机制
- 不再针对特定时间段做不同处理

### 2. 优化超时时间
- **停止连接超时**：从5秒减少到3秒
- **连接操作超时**：从10秒减少到5秒
- **重试间隔**：从3秒减少到1秒
- **看门狗检查间隔**：从5秒减少到2秒

### 3. 减少等待时间
- **xt_trader创建后等待**：从0.5秒减少到0.2秒
- **xt_trader启动后等待**：从0.5秒减少到0.2秒
- **连接停止后等待**：从1秒减少到0.5秒

### 4. 限制重试次数
- **最大重试次数**：从10次减少到5次
- **最大重连时间**：从60秒增加到120秒（2分钟）

## 关键改进

### 重连看门狗优化
```python
def reconnect_watchdog():
    """重连看门狗，防止重连过程卡住"""
    while time.time() - reconnect_start_time < max_reconnect_time:
        time.sleep(2)  # 每2秒检查一次，更及时
        if not self.is_reconnecting:
            return  # 重连已完成
```

### 连接操作超时优化
```python
# 等待连接结果，最多等待5秒
try:
    result = connect_queue.get(timeout=5)
    # 处理结果...
except queue.Empty:
    self.logger.warning(f"[重连] 连接操作超时 (第{retry_count}次)")
    connect_result = -1
```

### 停止连接超时优化
```python
# 等待停止操作完成，最多等待3秒
try:
    result = stop_queue.get(timeout=3)
    # 处理结果...
except queue.Empty:
    self.logger.warning("[重连] 停止连接超时，继续重连流程")
```

## 修改的文件
- `brokers/qmt_adapter.py` - 重连逻辑优化

## 预期效果
1. **避免程序卡死**：通过更短的超时时间和更及时的看门狗检查
2. **快速重连**：减少各种等待时间，加快重连速度
3. **统一处理**：不再针对特定时间段，任何时候都使用相同的重连策略
4. **更好的错误处理**：更短的超时时间避免长时间阻塞

## 核心原则
- **简单优于复杂**：移除所有时间段相关的复杂逻辑
- **快速失败**：使用更短的超时时间，避免长时间阻塞
- **及时恢复**：看门狗每2秒检查一次，更及时发现问题
- **统一策略**：任何时候都使用相同的重连机制

这个简化方案应该能够解决QMT重启时程序卡死的问题，无论何时发生重启都能快速恢复。
