# Tick数据获取优化说明

## 问题描述

原来的代码在近期分析功能中，一次性获取最近30个交易日的数据，这会导致：
1. 内存占用过大
2. 数据加载速度慢
3. 网络请求压力大
4. 用户体验差

## 优化方案

### 1. 修改 `ui/dialogs.py` 中的 `get_available_dates_and_data` 方法

**原来的实现：**
- 一次性获取60天的数据（包含30个交易日）
- 在内存中按日期分割数据
- 处理大量数据时内存占用高

**优化后的实现：**
- 先计算最近30个交易日
- 逐个日期获取数据
- 及时清理内存，避免内存累积

### 2. 优化 `core/backtest_engine.py` 中的 `load_data` 方法

**新增功能：**
- 区分单日数据和多日数据的处理逻辑
- 单日数据只获取交易时段（9:30-15:00）
- 减少等待时间，提高效率
- 添加更详细的日志信息

## 主要修改内容

### ui/dialogs.py
```python
def get_available_dates_and_data(self):
    """按天获取最近30个交易日的数据"""
    # 1. 计算最近30个交易日
    # 2. 逐个日期获取数据
    # 3. 及时清理内存
    # 4. 返回过滤后的数据
```

### core/backtest_engine.py
```python
def load_data(self, start_date, end_date, save_to_file=True):
    # 1. 区分单日和多日数据
    # 2. 单日数据优化时间范围
    # 3. 减少等待时间
    # 4. 添加详细日志
```

## 优化效果

1. **内存使用优化：**
   - 原来：一次性加载60天数据，内存占用大
   - 现在：按天加载，及时清理，内存占用小

2. **速度提升：**
   - 原来：需要等待大量数据下载和处理
   - 现在：逐个处理，响应更快

3. **稳定性提升：**
   - 原来：大数据量可能导致内存不足
   - 现在：内存使用可控，更稳定

4. **用户体验：**
   - 原来：长时间等待，可能卡死
   - 现在：有进度提示，响应及时

## 测试

创建了 `test_daily_data_loading.py` 测试脚本来验证优化效果。

## 注意事项

1. 确保 `chncal` 模块正常工作，用于判断交易日
2. 网络连接稳定，避免单日数据获取失败
3. 如果某个日期数据获取失败，会跳过该日期继续处理

## 兼容性

修改后的代码保持了原有的接口不变，只是内部实现更加高效，不会影响其他功能的使用。
