# 策略停止问题修复方案

## 问题分析

### 问题描述
从日志可以看到：
1. **09:41:42** - 603280.SH 上涨触发买入操作，基准价从 38.75 更新为 39.50
2. **09:41:42** - 显示"[603280.SH] 循环次数为0，执行一次后结束"
3. **09:41:45** - 但是策略还在继续处理tick数据，说明策略没有停止

### 根本原因
策略进程的主循环是阻塞式的，没有超时机制。当策略发送停止信号时，策略进程可能没有及时接收到停止信号。

**具体问题**：
1. **策略发送停止信号**：策略通过`self.control_pipe.send('stop')`发送停止信号
2. **策略进程正在等待接收消息**：策略进程通过`self.control_pipe.recv()`等待消息
3. **管道通信问题**：策略进程可能没有及时接收到自己发送的停止信号

## 修复方案

### 1. 添加超时机制
在策略进程的主循环中添加超时机制，避免阻塞太久：

```python
# 添加超时机制，避免阻塞太久
import select
import sys

# 检查管道是否有数据可读
if hasattr(select, 'select'):
    # Unix系统使用select
    ready, _, _ = select.select([self.control_pipe], [], [], 1.0)  # 1秒超时
    if not ready:
        # 超时，继续循环
        continue
else:
    # Windows系统，使用非阻塞方式
    try:
        message = self.control_pipe.recv()
    except (EOFError, BrokenPipeError):
        self.logger.warning(f"[{self.stock_code}] 控制管道已关闭")
        break
    except:
        # 没有数据可读，继续循环
        continue
```

### 2. 修复逻辑
- **Unix系统**：使用`select.select()`检查管道是否有数据可读，设置1秒超时
- **Windows系统**：使用非阻塞方式，如果没有数据可读则继续循环
- **超时处理**：如果超时，继续循环，避免策略进程卡死

## 预期效果

### 修复前
1. 策略发送停止信号后，策略进程可能没有及时接收到停止信号
2. 策略进程继续处理tick数据，没有停止
3. 用户需要手动停止任务

### 修复后
1. 策略发送停止信号后，策略进程能够及时接收到停止信号
2. 策略进程立即停止，不再处理tick数据
3. 任务自动停止，用户无需手动干预

## 修改的文件
1. `strategies/base_strategy.py` - 基础策略类，添加超时机制

## 测试场景
1. **循环次数为0的任务**：执行一次操作后应该自动停止
2. **正常停止的任务**：用户手动停止任务时应该立即停止
3. **异常情况**：策略进程出现异常时应该能够正常退出

## 技术细节

### 超时机制
- **Unix系统**：使用`select.select()`检查管道状态
- **Windows系统**：使用异常处理机制实现非阻塞
- **超时时间**：设置为1秒，平衡响应速度和资源消耗

### 兼容性
- **跨平台支持**：同时支持Unix和Windows系统
- **向后兼容**：不影响现有功能
- **性能优化**：避免不必要的资源消耗

这个修复确保了策略进程能够及时响应停止信号，解决了"循环次数为0，执行一次后结束"但策略没有停止的问题。
