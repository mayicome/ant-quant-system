# Flask相关依赖（用于multi_port_web.py）
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.3

# PyQt5相关依赖（用于main.py GUI界面）
PyQt5>=5.15.0

# 数据处理依赖
pandas>=1.5.0
numpy>=1.23.0
# 读取 .xls（pandas 2.x 要求 xlrd>=2.0.1；.xlsx 走 openpyxl）
xlrd>=2.0.1
# 非交易日手动补跑：把分析脚本的「今天」冻到目标交易日
freezegun>=1.5.1

# 系统工具
psutil>=5.9.0

# get_truth_following.py 贴文中文翻译（可选）
deep-translator>=1.11.4

# 中国交易日历
# 注意：已使用 akshare 替代 chncal 用于交易日判断
# akshare>=1.0.0

# 东财涨停/主力净流入抓取（Selenium）
selenium>=4.15.0

# QMT量化交易接口
# 注意：xtquant由QMT客户端提供，安装QMT客户端后会自动包含
# 如果通过pip安装，可能需要从QMT官方源获取
# xtquant
