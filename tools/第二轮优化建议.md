# 第二轮优化建议（基于新混淆矩阵）

## 当前状态分析

### 改善情况：
- ✅ **试盘**: 从 0% 提升到 **80%** (4/5) - 显著改善
- ✅ **诱多出货**: 从 0% 提升到 **66.7%** (2/3) - 显著改善
- ✅ **整体准确率**: 从 26.09% 提升到 **30.43%**

### 新出现的问题：
- ❌ **强势封板**: 从 54.5% 降到 **9.1%** (1/11) - 7个被误判为诱多出货
- ❌ **洗盘**: 从 75% 降到 **0%** (0/4) - 全部被误判

## 问题根源

从最优参数可以看出：
- `strong_seal_bias: -20.0` - 强势封板被过度减分
- `wash_bias: -20.0` + `wash_weight: 0.3` - 洗盘被过度压制
- `distribution_bias: 10.0` - 诱多出货被加分，导致强势封板被误判为诱多出货

## 优化策略

### 策略1：微调偏移量（推荐）

**当前参数调整方向：**

```
强势封板偏移量：从 -20.0 调整到 -10 ~ 0
- strong_seal_bias: -10 ~ 5 (步长2.5)

洗盘偏移量：从 -20.0 调整到 -10 ~ 0
- wash_bias: -10 ~ 0 (步长2.5)

诱多出货偏移量：从 10.0 调整到 0 ~ 10
- distribution_bias: 0 ~ 10 (步长2.5)

试盘偏移量：保持或微调
- test_bias: 15 ~ 25 (步长2.5)
```

### 策略2：调整权重平衡

**在保持偏移量的基础上，微调权重：**

```
强势封板权重：稍微提高
- strong_seal_weight: 1.0 ~ 1.3 (步长0.1)

洗盘权重：稍微提高（当前0.3太低）
- wash_weight: 0.5 ~ 0.8 (步长0.1)

诱多出货权重：保持或微调
- distribution_weight: 0.9 ~ 1.2 (步长0.1)

试盘权重：保持
- test_weight: 1.4 ~ 1.6 (步长0.1)
```

### 策略3：调整最小得分差异阈值

**当前 min_score_diff: 7.0 可能过高，尝试降低：**

```
min_score_diff: 3.0 ~ 7.0 (步长1.0)
```

### 策略4：组合优化（最全面）

**同时优化权重和偏移量，范围更精细：**

```
权重参数（精细调整）：
- strong_seal_weight: 1.0 ~ 1.3 (步长0.1)
- test_weight: 1.4 ~ 1.6 (步长0.1)
- wash_weight: 0.5 ~ 0.8 (步长0.1)
- distribution_weight: 0.9 ~ 1.2 (步长0.1)

偏移量参数（精细调整）：
- strong_seal_bias: -10 ~ 5 (步长2.5)
- test_bias: 15 ~ 25 (步长2.5)
- wash_bias: -10 ~ 0 (步长2.5)
- distribution_bias: 0 ~ 10 (步长2.5)

全局参数：
- normalize: True (保持)
- min_score_diff: 3.0 ~ 7.0 (步长1.0)
```

## 推荐优化步骤

### 第一步：快速微调偏移量（最快见效）

在参数表格中，将偏移量参数范围调整为：

```
strong_seal_bias: -10 ~ 5 (步长2.5)
wash_bias: -10 ~ 0 (步长2.5)
distribution_bias: 0 ~ 10 (步长2.5)
test_bias: 15 ~ 25 (步长2.5)
```

保持其他参数不变，重新运行优化。

### 第二步：如果第一步效果不理想，调整权重

```
strong_seal_weight: 1.0 ~ 1.3
wash_weight: 0.5 ~ 0.8
distribution_weight: 0.9 ~ 1.2
test_weight: 1.4 ~ 1.6
```

### 第三步：调整最小得分差异阈值

```
min_score_diff: 3.0 ~ 7.0 (步长1.0)
```

## 预期目标

- **强势封板识别准确率**: 从 9.1% 提升到至少 50%
- **洗盘识别准确率**: 从 0% 提升到至少 50%
- **试盘识别准确率**: 保持 80% 左右
- **诱多出货识别准确率**: 保持 60% 以上
- **整体准确率**: 从 30.43% 提升到至少 50%

## 关键洞察

1. **平衡是关键**：不能过度压制某个行为，否则会导致其他行为被误判
2. **偏移量比权重更敏感**：小幅调整偏移量就能产生明显效果
3. **需要多次迭代**：逐步微调，找到最佳平衡点

