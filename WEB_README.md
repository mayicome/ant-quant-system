# 关键价格计算Web应用

这是一个基于Flask的Web应用，提供关键价格计算功能，用户可以通过浏览器访问和使用。

## 功能特点

- 🌐 **Web界面**: 现代化的H5页面，支持移动端访问
- 📱 **响应式设计**: 自适应各种屏幕尺寸
- 🎨 **美观界面**: 渐变色彩和流畅动画效果
- ⚡ **实时计算**: 输入股票代码即可获取关键价格点
- 📊 **详细数据**: 显示5日线、10日线重合点等关键价格信息

## 安装和运行

### 方法一：使用启动脚本（推荐）

1. 确保在项目根目录下
2. 运行启动脚本：
   ```bash
   python start_web.py
   ```

### 方法二：手动启动

1. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```

2. 启动应用：
   ```bash
   python web_app.py
   ```

## 访问应用

启动成功后，在浏览器中访问：
- 本地访问：http://localhost:5000
- 局域网访问：http://[你的IP地址]:5000

## 使用说明

1. 在输入框中输入股票代码（如：000001）
2. 点击"计算关键价格点"按钮
3. 等待计算完成，查看结果

## 技术架构

- **后端**: Flask + Python
- **前端**: HTML5 + CSS3 + JavaScript
- **数据源**: KeyPriceCalculator（独立计算模块）+ QMT适配器
- **特点**: 不依赖QWidget，需要QMT适配器正常工作

## 文件结构

```
├── web_app.py              # Flask应用主文件
├── key_price_calculator.py # 关键价格计算模块（Web版本）
├── start_web.py            # 启动脚本
├── requirements.txt         # Python依赖
├── templates/              # HTML模板目录
│   └── index.html         # 主页面模板
└── WEB_README.md          # 说明文档
```

## 注意事项

1. **QMT适配器**: 必须正常工作，否则Web应用无法启动
2. **依赖安装**: 首次运行可能需要安装依赖
3. **端口占用**: 默认使用5000端口，如有冲突请修改web_app.py中的端口号
4. **Python版本**: 建议在Python 3.7+环境下运行

## 故障排除

### 常见问题

1. **端口被占用**
   - 修改web_app.py中的端口号
   - 或关闭占用5000端口的程序

2. **依赖安装失败**
   - 检查网络连接
   - 尝试使用国内镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/`

3. **QMT适配器初始化失败**
   - 检查QMT软件是否正常运行
   - 确认相关模块文件存在

4. **导入模块失败**
   - 确保在项目根目录下运行
   - 检查Python路径设置

## 更新日志

- v1.0.0: 初始版本，支持基本的关键价格计算功能

## 许可证

本项目基于现有项目许可证。
